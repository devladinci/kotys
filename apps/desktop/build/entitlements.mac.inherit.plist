<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Electron Framework is signed by a different (non-Apple) identity;
         with a self-signed dev cert there is no shared Team ID, so library
         validation must be off or dyld kills every helper at launch. -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Helpers sign separately from the main bundle; getUserMedia runs in
         the renderer helper, so it needs mic access of its own. -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
  </dict>
</plist>