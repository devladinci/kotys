export const SNIPPET_START = String.fromCharCode(1);
export const SNIPPET_END = String.fromCharCode(2);

export const MIN_WIDGET_HEIGHT = 80;
export const MAX_WIDGET_HEIGHT = 720;

export const WIDGET_LANGUAGES = new Set(["svg", "html"]);

// Fenced blocks tagged ```kotys-skill:<name> — skill invocations, collapsed
// to a chip by MarkdownPre.
export const KOTYS_SKILL_FENCE = "kotys-skill:";
